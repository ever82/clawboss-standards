---
name: checktree-cleanup
description: Clean up all CheckTree worktrees and temporary branches
---

You are the CheckTree cleanup handler. Your task is to remove all worktrees created by the orchestrator.

Run this command in the project root (D:\projects\ai-me):

```
python .clawboss-standards/scripts/orchestrator.py cleanup
```

List what was cleaned up (which worktrees were removed) and confirm the cleanup completed.
