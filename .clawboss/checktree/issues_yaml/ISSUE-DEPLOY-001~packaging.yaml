id: "ISSUE-DEPLOY-001"
issue_number: "001"
title: "打包与分发"
slug: "packaging"
status: "pending"
completion: 0
description: |
  实现 AI-Me 系统的打包和分发机制，支持多种平台（Windows、macOS、Linux）
  和多种安装方式（npm、独立二进制、包管理器）。确保用户能够方便快捷地
  安装和更新系统。
category: "infrastructure"
is_parent: false
prerequisites:
  - "ISSUE-INFRA-004"
related_specs:
  - "SPEC-DEPLOY-001"
created_at: "2026-03-31"
completed_at: null

criteria:
  - id: "ISSUE-DEPLOY-001~criterion-1"
    name: "npm 包发布"
    description: |
      实现 npm 包的构建和发布流程：
      - package.json 配置优化
      - 预编译二进制分发（optionalDependencies）
      - 多平台支持配置（os、cpu 字段）
      - 自动化发布脚本
    category: "infrastructure"
    status: "pending"
    coverage:
      unit: not_needed
      integration: sufficient
      e2e: sufficient
      ai: not_needed
      manual: sufficient

  - id: "ISSUE-DEPLOY-001~criterion-2"
    name: "独立二进制打包"
    description: |
      实现独立可执行文件的打包：
      - pkg/vercel/pkg 或 nexe 集成
      - 多平台二进制构建（x64、arm64）
      - 二进制压缩和优化
      - 代码签名（Windows、macOS）
    category: "infrastructure"
    status: "pending"
    coverage:
      unit: not_needed
      integration: sufficient
      e2e: sufficient
      ai: not_needed
      manual: sufficient
    note: "需在 CI 中配置签名证书"

  - id: "ISSUE-DEPLOY-001~criterion-3"
    name: "安装脚本"
    description: |
      提供便捷的安装脚本：
      - curl | bash 一键安装（Unix）
      - PowerShell 安装脚本（Windows）
      - 版本选择支持（latest、specific、nightly）
      - 安装路径自定义
    category: "infrastructure"
    status: "pending"
    coverage:
      unit: not_needed
      integration: sufficient
      e2e: sufficient
      ai: not_needed
      manual: sufficient

  - id: "ISSUE-DEPLOY-001~criterion-4"
    name: "包管理器支持"
    description: |
      支持主流包管理器分发：
      - Homebrew（macOS、Linux）
      - Scoop/Chocolatey（Windows）
      - AUR（Arch Linux）
      - 各平台的 formula/manifest 维护
    category: "infrastructure"
    status: "pending"
    coverage:
      unit: not_needed
      integration: sufficient
      e2e: sufficient
      ai: not_needed
      manual: sufficient

  - id: "ISSUE-DEPLOY-001~criterion-5"
    name: "自动更新机制"
    description: |
      实现应用的自动更新检查：
      - 新版本检测
      - 更新通知（CLI、配置文件）
      - 增量更新支持
      - 更新日志展示
    category: "feature"
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: sufficient
      ai: not_needed
      manual: not_needed
