id: "ISSUE-INFRA-001"
issue_number: 1
title: "项目初始化和仓库搭建"
slug: "project-setup"
status: "pending"
description: |
  初始化 AI-Me 行为管理系统项目，创建代码仓库，配置基础开发环境。
  包括：Git 仓库初始化、LICENSE 文件、README 初始版本、.gitignore 配置、
  基础目录结构创建、Node.js/TypeScript 项目配置、包管理器配置等。
is_parent: false
prerequisites: []
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-001~criterion-1"
    name: "Git 仓库和基础文件配置完成"
    description: |
      创建 Git 仓库，配置 .gitignore（包含 node_modules, dist, .env 等），
      添加 LICENSE 文件（MIT 或合适的开源协议），创建初始 README.md
    status: "pending"
    coverage:
      unit: not_needed
      integration: not_needed
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      manual:
        instructions: |
          1. 验证 Git 仓库已初始化：git status 显示正常
          2. 检查 .gitignore 包含：node_modules/, dist/, .env, *.log, .DS_Store
          3. 验证 LICENSE 文件存在且协议明确
          4. 检查 README.md 包含项目名称、简介、快速开始指南
        required_tools: ["git", "bash"]
    execution_time: "<5 minutes"
    prerequisites: []

  - id: "ISSUE-INFRA-001~criterion-2"
    name: "Node.js 和 TypeScript 项目配置完成"
    description: |
      初始化 package.json，配置 TypeScript（tsconfig.json），
      设置源码目录结构（src/），配置开发脚本（dev, build, clean）
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: not_needed
    verification:
      unit:
        command: "npm test -- src/infra/project-setup.test.ts"
        test_files: ["src/infra/project-setup.test.ts"]
      integration:
        command: "npm test -- src/infra/project-setup.integration.test.ts"
        test_files: ["src/infra/project-setup.integration.test.ts"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-001~criterion-3"
    name: "开发工具链配置完成"
    description: |
      配置 ESLint 代码检查、Prettier 代码格式化、Husky git hooks、
      lint-staged 预提交检查，确保代码质量和一致性
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/dev-tools.test.ts"
        test_files: ["src/infra/dev-tools.test.ts"]
      integration:
        command: "npm test -- src/infra/dev-tools.integration.test.ts"
        test_files: ["src/infra/dev-tools.integration.test.ts"]
      manual:
        instructions: |
          1. 运行 npm run lint 验证 ESLint 配置正确
          2. 运行 npm run format 验证 Prettier 配置正确
          3. 尝试提交代码验证 Husky hooks 正常工作
          4. 验证 lint-staged 在暂存文件时触发检查
        required_tools: ["npm", "git", "bash"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-001~criterion-2"]

  - id: "ISSUE-INFRA-001~criterion-4"
    name: "项目文档结构初始化完成"
    description: |
      创建项目文档目录结构，包含：架构文档位置、API 文档位置、
      开发指南位置、.clawboss 目录结构创建
    status: "pending"
    coverage:
      unit: not_needed
      integration: not_needed
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      manual:
        instructions: |
          1. 验证 .clawboss/ 目录存在，包含 checktree/ 和 spec/ 子目录
          2. 检查 docs/ 或 .clawboss/spec/ 目录结构完整
          3. 验证目录命名符合项目规范
        required_tools: ["bash", "ls"]
    execution_time: "<5 minutes"
    prerequisites: []

  - id: "ISSUE-INFRA-001~criterion-5"
    name: "基础依赖安装和版本锁定"
    description: |
      安装核心运行时依赖和开发依赖，生成 package-lock.json，
      确保依赖版本可控和可重现构建
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/deps.test.ts"
        test_files: ["src/infra/deps.test.ts"]
      integration:
        command: "npm test -- src/infra/deps.integration.test.ts"
        test_files: ["src/infra/deps.integration.test.ts"]
      manual:
        instructions: |
          1. 验证 package-lock.json 存在且最新
          2. 运行 npm ci 验证依赖可干净安装
          3. 检查关键依赖版本符合设计要求
        required_tools: ["npm", "bash"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-001~criterion-2"]
