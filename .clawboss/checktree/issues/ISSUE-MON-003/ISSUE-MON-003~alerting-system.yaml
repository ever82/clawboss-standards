id: "ISSUE-MON-003"
issue_number: 3
title: "告警系统"
slug: "alerting-system"
status: "pending"
description: "构建完整的告警系统，支持基于指标阈值和业务规则的告警触发、告警级别划分（INFO/WARNING/CRITICAL）、告警通知渠道（日志、CLI、Webhook、邮件）、告警抑制与聚合、告警历史记录与查询，以及与监控指标和健康检查的联动。"
is_parent: false
prerequisites:
  - "ISSUE-MON-001"
  - "ISSUE-CORE-009"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-MON-003~criterion-1"
    name: "告警规则引擎核心实现"
    description: "实现告警规则引擎，支持基于指标阈值的规则（>、<、=、!=）、复合条件规则（AND、OR、NOT）、时间窗口条件（持续N分钟触发）、以及规则优先级和启用/禁用控制"
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: sufficient
      ai: sufficient
      manual: not_needed
    verification:
      unit:
        command: "npm test -- src/monitoring/alert/rule-engine.test.ts"
        test_files: ["src/monitoring/alert/rule-engine.test.ts"]
      integration:
        command: "npm test -- src/monitoring/alert/rule-engine.integration.test.ts"
        test_files: ["src/monitoring/alert/rule-engine.integration.test.ts"]
      e2e:
        command: "npm run test:e2e -- alert-rule-engine"
        test_files: ["e2e/alert-rule-engine.spec.ts"]
      ai:
        prompt: "请验证告警规则引擎核心实现:
1. 检查规则定义结构（name, condition, severity, enabled, cooldown等字段）
2. 验证比较运算符支持（>, <, >=, <=, =, !=, in, not in）
3. 检查复合条件支持（AND、OR、NOT逻辑组合）
4. 验证时间窗口条件（for: '5m'表示持续5分钟才触发）
5. 检查规则优先级处理（priority字段，高优先级优先评估）
6. 验证规则启用/禁用控制
7. 检查规则评估性能（避免重复计算，缓存中间结果）
返回: {"passed": true/false, "reason": "详细说明"}"
        context_files: ["src/monitoring/alert/rule-engine.ts", "src/monitoring/alert/types.ts"]
        model: "claude-sonnet-4-6"
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-MON-003~criterion-2"
    name: "告警级别与分类体系"
    description: "实现告警级别体系（INFO信息/WARNING警告/CRITICAL严重/EMERGENCY紧急）和告警分类（性能/可用性/业务/安全），支持不同级别和分类的差异化处理策略"
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: sufficient
      ai: sufficient
      manual: not_needed
    verification:
      unit:
        command: "npm test -- src/monitoring/alert/severity.test.ts"
        test_files: ["src/monitoring/alert/severity.test.ts"]
      integration:
        command: "npm test -- src/monitoring/alert/severity.integration.test.ts"
        test_files: ["src/monitoring/alert/severity.integration.test.ts"]
      e2e:
        command: "npm run test:e2e -- alert-severity"
        test_files: ["e2e/alert-severity.spec.ts"]
      ai:
        prompt: "请验证告警级别与分类体系:
1. 检查告警级别枚举（INFO/WARNING/CRITICAL/EMERGENCY）
2. 验证级别数值映射（用于比较，如EMERGENCY=4, INFO=1）
3. 检查告警分类体系（PERFORMANCE/AVAILABILITY/BUSINESS/SECURITY）
4. 验证不同级别的默认通知渠道（如CRITICAL同时发日志和Webhook）
5. 检查级别升级机制（长时间未处理的WARNING升级为CRITICAL）
6. 验证分类标签的自动添加（基于规则类型自动分类）
7. 检查级别和分类的筛选查询支持
返回: {"passed": true/false, "reason": "详细说明"}"
        context_files: ["src/monitoring/alert/severity.ts", "src/monitoring/alert/category.ts"]
        model: "claude-sonnet-4-6"
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-MON-003~criterion-3"
    name: "告警通知渠道实现"
    description: "实现多种告警通知渠道，包括日志记录（INFRA-006B集成）、CLI输出（桌面通知）、Webhook回调、邮件通知（可选），支持渠道路由配置"
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: sufficient
      ai: sufficient
      manual: not_needed
    verification:
      unit:
        command: "npm test -- src/monitoring/alert/channels.test.ts"
        test_files: ["src/monitoring/alert/channels.test.ts"]
      integration:
        command: "npm test -- src/monitoring/alert/channels.integration.test.ts"
        test_files: ["src/monitoring/alert/channels.integration.test.ts"]
      e2e:
        command: "npm run test:e2e -- alert-channels"
        test_files: ["e2e/alert-channels.spec.ts"]
      ai:
        prompt: "请验证告警通知渠道实现:
1. 检查日志渠道实现（结构化日志输出，含alert_id, severity, message）
2. 验证CLI输出渠道（格式化终端输出，颜色区分级别）
3. 检查Webhook渠道（HTTP POST回调，支持自定义headers，超时配置）
4. 验证邮件通知渠道（SMTP配置，模板渲染，可选实现）
5. 检查渠道路由配置（不同级别走不同渠道，如CRITICAL走所有渠道）
6. 验证渠道失败处理（单个渠道失败不影响其他渠道）
7. 检查渠道速率限制（防止通知风暴）
返回: {"passed": true/false, "reason": "详细说明"}"
        context_files: ["src/monitoring/alert/channels/index.ts", "src/monitoring/alert/channels/webhook.ts"]
        model: "claude-sonnet-4-6"
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-MON-003~criterion-4"
    name: "告警抑制与聚合机制"
    description: "实现告警抑制机制（冷却期、去重、静默窗口）和告警聚合（同一问题多次触发合并、相关告警分组），避免告警风暴"
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: sufficient
      ai: sufficient
      manual: not_needed
    verification:
      unit:
        command: "npm test -- src/monitoring/alert/suppression.test.ts"
        test_files: ["src/monitoring/alert/suppression.test.ts"]
      integration:
        command: "npm test -- src/monitoring/alert/suppression.integration.test.ts"
        test_files: ["src/monitoring/alert/suppression.integration.test.ts"]
      e2e:
        command: "npm run test:e2e -- alert-suppression"
        test_files: ["e2e/alert-suppression.spec.ts"]
      ai:
        prompt: "请验证告警抑制与聚合机制:
1. 检查冷却期实现（cooldown: 5m，触发后5分钟内不重复发送）
2. 验证去重机制（相同告警指纹的去重计数）
3. 检查静默窗口配置（maintenance window期间不发送告警）
4. 验证告警聚合（同一规则多次触发合并为一条，含count字段）
5. 检查相关告警分组（按category或source分组聚合）
6. 验证抑制规则优先级（全局抑制>规则级抑制）
7. 检查抑制状态查询接口（查看当前被抑制的告警）
返回: {"passed": true/false, "reason": "详细说明"}"
        context_files: ["src/monitoring/alert/suppression.ts", "src/monitoring/alert/aggregation.ts"]
        model: "claude-sonnet-4-6"
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-MON-003~criterion-5"
    name: "告警与指标/健康检查集成"
    description: "实现告警系统与指标收集（MON-001）和健康检查（MON-002）的集成，支持基于指标数据自动触发告警、基于健康状态变化触发告警"
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: sufficient
      ai: sufficient
      manual: not_needed
    verification:
      unit:
        command: "npm test -- src/monitoring/alert/integration.test.ts"
        test_files: ["src/monitoring/alert/integration.test.ts"]
      integration:
        command: "npm test -- src/monitoring/alert/integration.integration.test.ts"
        test_files: ["src/monitoring/alert/integration.integration.test.ts"]
      e2e:
        command: "npm run test:e2e -- alert-integration"
        test_files: ["e2e/alert-integration.spec.ts"]
      ai:
        prompt: "请验证告警与指标/健康检查集成:
1. 检查指标数据订阅机制（订阅MetricsCollector的数据流）
2. 验证基于指标阈值的自动告警触发（CPU>80%触发告警）
3. 检查健康状态变化事件监听（订阅HealthStatusChangedEvent）
4. 验证健康检查失败自动告警（组件UNHEALTHY时触发）
5. 检查集成配置（启用/禁用自动告警，规则覆盖）
6. 验证告警恢复通知（指标恢复正常或组件恢复时发送）
7. 检查集成测试覆盖（模拟指标数据验证告警触发）
返回: {"passed": true/false, "reason": "详细说明"}"
        context_files: ["src/monitoring/alert/integration.ts", "src/monitoring/metrics/collector.ts", "src/monitoring/health/health-checker.ts"]
        model: "claude-sonnet-4-6"
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-MON-003~criterion-6"
    name: "告警历史记录与CLI管理"
    description: "实现告警历史记录持久化、告警查询与过滤接口、告警统计报表，以及CLI告警管理命令（查看、确认、静默、历史查询）"
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: sufficient
      ai: sufficient
      manual: not_needed
    verification:
      unit:
        command: "npm test -- src/monitoring/alert/history.test.ts"
        test_files: ["src/monitoring/alert/history.test.ts"]
      integration:
        command: "npm test -- src/monitoring/alert/history.integration.test.ts"
        test_files: ["src/monitoring/alert/history.integration.test.ts"]
      e2e:
        command: "npm run test:e2e -- alert-history"
        test_files: ["e2e/alert-history.spec.ts"]
      ai:
        prompt: "请验证告警历史记录与CLI管理:
1. 检查告警历史记录结构（id, ruleName, severity, message, timestamp, status, resolvedAt）
2. 验证告警持久化存储（支持SQLite/文件/内存）
3. 检查告警查询接口（按时间范围、级别、分类、状态过滤）
4. 验证告警统计报表（按日/周/月统计告警数量、TopN告警规则）
5. 检查CLI告警命令实现：
   - `ai-me alert list` - 列出当前活跃告警
   - `ai-me alert history` - 查询历史告警
   - `ai-me alert ack <id>` - 确认告警
   - `ai-me alert silence <rule>` - 静默指定规则
6. 验证告警状态流转（active -> acknowledged -> resolved）
7. 检查历史数据清理策略（保留N天，自动清理）
返回: {"passed": true/false, "reason": "详细说明"}"
        context_files: ["src/monitoring/alert/history.ts", "src/cli/commands/alert.ts"]
        model: "claude-sonnet-4-6"
    execution_time: "<60 seconds"
    prerequisites: []
