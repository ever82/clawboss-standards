id: "ISSUE-INFRA-006B"
issue_number: 6
title: "日志系统"
slug: "logging-system"
status: "pending"
description: |
  实现统一的日志系统，支持：多级别日志（DEBUG, INFO, WARN, ERROR）、
  结构化日志输出、多目标输出（文件、控制台、远程）、
  日志轮转、敏感信息脱敏、性能影响最小化。
is_parent: false
prerequisites:
  - "ISSUE-INFRA-001"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-006B~criterion-1"
    name: "日志基础框架实现完成"
    description: |
      实现日志核心功能：
      - Logger 类定义
      - 日志级别支持（DEBUG, INFO, WARN, ERROR, FATAL）
      - 日志格式化（时间、级别、消息、上下文）
      - 日志过滤器（按级别、模块）
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/logger-core.test.ts"
        test_files: ["src/infra/logger-core.test.ts"]
      integration:
        command: "npm test -- src/infra/logger-core.integration.test.ts"
        test_files: ["src/infra/logger-core.integration.test.ts"]
      manual:
        instructions: |
          1. 验证 Logger 类可实例化
          2. 测试各级别日志输出（logger.info(), logger.error()）
          3. 检查输出格式包含时间戳、级别、消息
          4. 验证过滤器正确过滤低级别日志
          5. 检查日志级别可动态调整
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-006B~criterion-2"
    name: "结构化日志输出实现完成"
    description: |
      实现 JSON 结构化日志：
      - JSON 格式日志输出
      - 标准字段（timestamp, level, message, module, traceId）
      - 可扩展字段支持
      - 嵌套对象序列化
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/logger-structured.test.ts"
        test_files: ["src/infra/logger-structured.test.ts"]
      integration:
        command: "npm test -- src/infra/logger-structured.integration.test.ts"
        test_files: ["src/infra/logger-structured.integration.test.ts"]
      manual:
        instructions: |
          1. 配置日志输出为 JSON 格式
          2. 验证输出是可解析的有效 JSON
          3. 检查包含标准字段（timestamp, level, message）
          4. 测试自定义字段正确序列化
          5. 验证嵌套对象正确输出
        required_tools: ["npm", "bash", "jq"]
    execution_time: "<30 seconds"
    prerequisites: ["ISSUE-INFRA-006B~criterion-1"]

  - id: "ISSUE-INFRA-006B~criterion-3"
    name: "多目标日志输出完成"
    description: |
      实现多输出目标：
      - 控制台输出（带颜色）
      - 文件输出（按级别分离）
      - 异步批量写入
      - 输出目标可配置组合
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/logger-transports.test.ts"
        test_files: ["src/infra/logger-transports.test.ts"]
      integration:
        command: "npm test -- src/infra/logger-transports.integration.test.ts"
        test_files: ["src/infra/logger-transports.integration.test.ts"]
      manual:
        instructions: |
          1. 配置多目标输出（console + file）
          2. 验证控制台输出带颜色
          3. 检查日志文件正确写入
          4. 测试按级别分离文件（error.log, combined.log）
          5. 验证异步写入不阻塞主流程
        required_tools: ["npm", "bash", "cat"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-006B~criterion-4"
    name: "日志轮转和归档完成"
    description: |
      实现日志文件管理：
      - 按日期/大小轮转
      - 压缩旧日志
      - 自动清理（保留 N 天）
      - 文件权限控制
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/logger-rotation.test.ts"
        test_files: ["src/infra/logger-rotation.test.ts"]
      integration:
        command: "npm test -- src/infra/logger-rotation.integration.test.ts"
        test_files: ["src/infra/logger-rotation.integration.test.ts"]
      manual:
        instructions: |
          1. 配置小文件阈值测试轮转（如 1KB）
          2. 写入日志触发轮转，验证新文件创建
          3. 检查旧日志文件被压缩（.gz）
          4. 验证超期日志自动删除
          5. 检查日志文件权限设置正确（644）
        required_tools: ["npm", "bash", "ls -la"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-006B~criterion-3"]

  - id: "ISSUE-INFRA-006B~criterion-5"
    name: "日志敏感信息脱敏完成"
    description: |
      实现日志安全处理：
      - 敏感字段识别（password, token, secret）
      - 自动脱敏处理（***）
      - 自定义脱敏规则
      - 防止日志注入攻击
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/logger-sanitization.test.ts"
        test_files: ["src/infra/logger-sanitization.test.ts"]
      integration:
        command: "npm test -- src/infra/logger-sanitization.integration.test.ts"
        test_files: ["src/infra/logger-sanitization.integration.test.ts"]
      manual:
        instructions: |
          1. 日志中包含敏感信息（password=mysecret123）
          2. 验证输出中密码被脱敏为 *** 或 [REDACTED]
          3. 测试 token、secret 等字段同样被脱敏
          4. 验证嵌套对象中的敏感字段也被处理
          5. 测试日志注入尝试被转义（如换行符）
        required_tools: ["npm", "bash", "cat"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-006B~criterion-6"
    name: "日志性能优化完成"
    description: |
      实现高性能日志：
      - 异步写入队列
      - 缓冲区管理
      - 日志级别短路（未启用级别不构造消息）
      - 性能指标（<1ms/日志）
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/logger-performance.test.ts"
        test_files: ["src/infra/logger-performance.test.ts"]
      integration:
        command: "npm test -- src/infra/logger-performance.integration.test.ts"
        test_files: ["src/infra/logger-performance.integration.test.ts"]
      manual:
        instructions: |
          1. 压力测试：10000 条日志写入
          2. 验证平均耗时 <1ms/条
          3. 检查内存使用稳定，无泄漏
          4. 测试高级别关闭时低级别无开销
          5. 验证异步队列正常消费
        required_tools: ["npm", "bash"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-006B~criterion-1"]
