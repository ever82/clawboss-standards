id: "ISSUE-INFRA-009"
issue_number: 9
title: "缓存层"
slug: "cache-layer"
status: "pending"
description: |
  实现多级缓存系统，支持：内存缓存、持久化缓存、
  缓存策略（LRU/TTL）、缓存一致性、缓存穿透/雪崩保护、
  缓存统计和监控。
is_parent: false
prerequisites:
  - "ISSUE-INFRA-004"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-009~criterion-1"
    name: "内存缓存实现完成"
    description: |
      实现内存缓存系统：
      - 键值存储接口
      - LRU 淘汰策略
      - 内存上限控制
      - 线程安全（Map 或 LRU Cache）
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/cache-memory.test.ts"
        test_files: ["src/infra/cache-memory.test.ts"]
      integration:
        command: "npm test -- src/infra/cache-memory.integration.test.ts"
        test_files: ["src/infra/cache-memory.integration.test.ts"]
      manual:
        instructions: |
          1. 验证内存缓存可存取键值
          2. 测试 LRU 淘汰（超出容量时最久未使用被移除）
          3. 检查内存使用不超过配置上限
          4. 验证并发访问安全
          5. 测试缓存命中率统计
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-009~criterion-2"
    name: "持久化缓存实现完成"
    description: |
      实现磁盘缓存系统：
      - 文件系统存储
      - SQLite 缓存表
      - 异步读写
      - 压缩存储选项
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/cache-persistent.test.ts"
        test_files: ["src/infra/cache-persistent.test.ts"]
      integration:
        command: "npm test -- src/infra/cache-persistent.integration.test.ts"
        test_files: ["src/infra/cache-persistent.integration.test.ts"]
      manual:
        instructions: |
          1. 写入缓存并验证磁盘文件创建
          2. 重启应用验证缓存数据可恢复
          3. 测试异步写入不阻塞读取
          4. 验证压缩选项减少磁盘占用
          5. 检查缓存文件权限设置
        required_tools: ["npm", "bash", "ls"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-009~criterion-3"
    name: "缓存策略实现完成"
    description: |
      实现缓存策略：
      - TTL（过期时间）支持
      - 惰性过期 vs 主动清理
      - 刷新策略（后台刷新）
      - 空值缓存（防止穿透）
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/cache-strategies.test.ts"
        test_files: ["src/infra/cache-strategies.test.ts"]
      integration:
        command: "npm test -- src/infra/cache-strategies.integration.test.ts"
        test_files: ["src/infra/cache-strategies.integration.test.ts"]
      manual:
        instructions: |
          1. 设置 TTL 测试过期自动失效
          2. 验证过期键被清理
          3. 测试后台刷新策略（接近过期时刷新）
          4. 验证空值缓存生效（null 结果也缓存）
          5. 检查 TTL 精度（误差 <1秒）
        required_tools: ["npm", "bash"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-009~criterion-1"]

  - id: "ISSUE-INFRA-009~criterion-4"
    name: "缓存一致性实现完成"
    description: |
      实现缓存与数据源一致性：
      - 写时更新（Write-through）
      - 写后失效（Cache-aside）
      - 批量失效
      - 版本号控制
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/cache-consistency.test.ts"
        test_files: ["src/infra/cache-consistency.test.ts"]
      integration:
        command: "npm test -- src/infra/cache-consistency.integration.test.ts"
        test_files: ["src/infra/cache-consistency.integration.test.ts"]
      manual:
        instructions: |
          1. 测试写时更新策略（写入同时更新缓存）
          2. 验证写后失效策略（写入后删除缓存）
          3. 测试批量失效接口
          4. 检查版本号冲突处理
          5. 验证最终一致性保证
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: ["ISSUE-INFRA-009~criterion-1"]

  - id: "ISSUE-INFRA-009~criterion-5"
    name: "缓存保护和统计完成"
    description: |
      实现缓存保护机制：
      - 缓存雪崩保护（并发回源控制）
      - 热点 Key 探测
      - 命中率/失效率统计
      - 缓存大小监控
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/cache-protection.test.ts"
        test_files: ["src/infra/cache-protection.test.ts"]
      integration:
        command: "npm test -- src/infra/cache-protection.integration.test.ts"
        test_files: ["src/infra/cache-protection.integration.test.ts"]
      manual:
        instructions: |
          1. 模拟并发回源，验证单飞请求（singleflight）
          2. 测试热点 Key 自动识别
          3. 验证命中率统计准确
          4. 检查缓存大小监控接口
          5. 测试缓存雪崩保护生效
        required_tools: ["npm", "bash"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-009~criterion-1"]
