id: "ISSUE-INFRA-002"
issue_number: 2
title: "目录结构定义和实现"
slug: "directory-structure"
status: "pending"
description: |
  定义并实现项目的完整目录结构规范，包括源码组织、测试文件位置、
  配置文件位置、资源文件管理等。确保目录结构清晰、可扩展、符合行业标准。
is_parent: false
prerequisites:
  - "ISSUE-INFRA-001"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-002~criterion-1"
    name: "源码目录结构设计完成"
    description: |
      设计并实现 src/ 目录结构，按模块分层组织：
      - core/：核心业务逻辑
      - infra/：基础设施代码
      - cli/：命令行接口
      - workspace/：工作空间管理
      - utils/：工具函数
      每个目录包含 index.ts 导出文件
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/directory-structure.test.ts"
        test_files: ["src/infra/directory-structure.test.ts"]
      integration:
        command: "npm test -- src/infra/directory-structure.integration.test.ts"
        test_files: ["src/infra/directory-structure.integration.test.ts"]
      manual:
        instructions: |
          1. 验证 src/ 下各子目录存在：core/, infra/, cli/, workspace/, utils/
          2. 每个目录包含 index.ts 或 index.js 导出文件
          3. 验证目录命名规范一致（小写、短横线分隔）
          4. 检查无循环依赖结构
        required_tools: ["bash", "tree"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-002~criterion-2"
    name: "测试目录结构设计完成"
    description: |
      实现测试文件组织规范：
      - 单元测试：*.test.ts 与源码同目录或 tests/unit/
      - 集成测试：tests/integration/ 或 *.integration.test.ts
      - E2E 测试：tests/e2e/
      - 测试 fixtures：tests/fixtures/
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/test-structure.test.ts"
        test_files: ["src/infra/test-structure.test.ts"]
      integration:
        command: "npm test -- src/infra/test-structure.integration.test.ts"
        test_files: ["src/infra/test-structure.integration.test.ts"]
      manual:
        instructions: |
          1. 验证 tests/ 目录存在，包含 unit/, integration/, e2e/, fixtures/
          2. 检查测试文件命名规范一致
          3. 验证测试工具配置正确识别各类型测试
        required_tools: ["bash", "npm"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-002~criterion-3"
    name: "配置和资源目录结构完成"
    description: |
      创建配置和资源目录：
      - config/：环境配置文件
      - assets/：静态资源文件
      - templates/：代码/文档模板
      - types/：全局类型定义
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/config-structure.test.ts"
        test_files: ["src/infra/config-structure.test.ts"]
      integration:
        command: "npm test -- src/infra/config-structure.integration.test.ts"
        test_files: ["src/infra/config-structure.integration.test.ts"]
      manual:
        instructions: |
          1. 验证 config/, assets/, templates/, types/ 目录存在
          2. 检查配置文件格式正确（JSON/YAML/TS）
          3. 验证类型定义文件可正确导出
        required_tools: ["bash"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-002~criterion-4"
    name: "目录结构文档和规范完成"
    description: |
      编写目录结构文档，说明各目录用途、文件放置规则、
      命名约定，确保团队成员遵循统一规范
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: sufficient
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/directory-docs.test.ts"
        test_files: ["src/infra/directory-docs.test.ts"]
      integration:
        command: "npm test -- src/infra/directory-docs.integration.test.ts"
        test_files: ["src/infra/directory-docs.integration.test.ts"]
      ai:
        prompt: |
          请验证目录结构文档的完整性和准确性：
          1. 文档是否清晰说明每个目录的用途？
          2. 是否包含文件命名规范（大小写、分隔符等）？
          3. 是否说明新增目录的审批流程？
          4. 是否提供常见场景的放置示例？
          返回格式：{"passed": true/false, "reason": "详细说明", "suggestions": ["改进建议"]}
        context_files: [".clawboss/spec/ARCHITECTURE.md", "README.md"]
        model: "claude-sonnet-4-6"
      manual:
        instructions: |
          1. 阅读 ARCHITECTURE.md 或 DIRECTORY.md 目录结构章节
          2. 验证文档包含：目录树示例、用途说明、命名规范
          3. 抽查 3 个文件位置是否符合文档规范
        required_tools: ["bash", "cat"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-002~criterion-1", "ISSUE-INFRA-002~criterion-2", "ISSUE-INFRA-002~criterion-3"]

  - id: "ISSUE-INFRA-002~criterion-5"
    name: "路径别名和模块解析配置完成"
    description: |
      配置 TypeScript 路径别名（@/core, @/infra 等），
      确保模块导入简洁、重构友好，配置 IDE 支持
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/path-alias.test.ts"
        test_files: ["src/infra/path-alias.test.ts"]
      integration:
        command: "npm test -- src/infra/path-alias.integration.test.ts"
        test_files: ["src/infra/path-alias.integration.test.ts"]
      manual:
        instructions: |
          1. 检查 tsconfig.json 中 paths 配置正确
          2. 验证运行时可正确解析路径别名
          3. 测试 IDE (VSCode) 可正确跳转到定义
          4. 检查构建输出正确处理路径别名
        required_tools: ["npm", "tsc", "code"]
    execution_time: "<60 seconds"
    prerequisites: []
