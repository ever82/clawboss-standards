id: "ISSUE-INFRA-007"
issue_number: 7
title: "测试基础设施"
slug: "test-infrastructure"
status: "pending"
description: |
  搭建完整的测试基础设施，包括：测试框架配置（Vitest）、
  测试数据库（SQLite in-memory）、Mock/Fake 工具、
  测试数据 Fixtures、覆盖率报告、CI 集成。
is_parent: false
prerequisites:
  - "ISSUE-INFRA-003B"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-007~criterion-1"
    name: "测试框架配置完成"
    description: |
      配置 Vitest 测试框架：
      - vitest.config.ts 配置
      - 测试匹配模式（*.test.ts, *.spec.ts）
      - 全局测试工具配置（expect, describe, it）
      - 测试环境设置（node）
      - 并行测试配置
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/vitest-config.test.ts"
        test_files: ["src/infra/vitest-config.test.ts"]
      integration:
        command: "npm test -- src/infra/vitest-config.integration.test.ts"
        test_files: ["src/infra/vitest-config.integration.test.ts"]
      manual:
        instructions: |
          1. 运行 npm test 验证测试框架启动
          2. 检查 vitest.config.ts 存在且配置正确
          3. 验证简单测试可通过
          4. 检查并行测试配置生效
          5. 验证全局工具（expect）可用
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-007~criterion-2"
    name: "测试数据库环境配置完成"
    description: |
      配置测试专用数据库：
      - SQLite in-memory 数据库
      - 测试数据库自动初始化和清理
      - 事务回滚隔离
      - 测试数据自动重置
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/test-database.test.ts"
        test_files: ["src/infra/test-database.test.ts"]
      integration:
        command: "npm test -- src/infra/test-database.integration.test.ts"
        test_files: ["src/infra/test-database.integration.test.ts"]
      manual:
        instructions: |
          1. 运行测试验证数据库自动创建
          2. 检查测试使用独立内存数据库
          3. 验证测试后数据自动清理
          4. 测试事务回滚机制
          5. 检查并行测试数据库隔离
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-007~criterion-3"
    name: "Mock 和 Fake 工具集完成"
    description: |
      实现测试辅助工具：
      - Mock 工具（vi.fn(), vi.mock()）
      - Fake 数据生成器
      - Stub 和 Spy 辅助函数
      - 时间控制（fake timers）
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/mock-tools.test.ts"
        test_files: ["src/infra/mock-tools.test.ts"]
      integration:
        command: "npm test -- src/infra/mock-tools.integration.test.ts"
        test_files: ["src/infra/mock-tools.integration.test.ts"]
      manual:
        instructions: |
          1. 测试 vi.fn() 创建 mock 函数
          2. 验证 vi.mock() 模块模拟
          3. 检查 Fake 数据生成器可用
          4. 测试 fake timers 控制时间
          5. 验证 spyOn 监控方法调用
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-007~criterion-4"
    name: "测试数据 Fixtures 完成"
    description: |
      创建测试数据管理：
      - Fixtures 目录结构（tests/fixtures/）
      - 基础数据工厂函数
      - 场景数据组合（测试场景）
      - 数据清理工具
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/test-fixtures.test.ts"
        test_files: ["src/infra/test-fixtures.test.ts"]
      integration:
        command: "npm test -- src/infra/test-fixtures.integration.test.ts"
        test_files: ["src/infra/test-fixtures.integration.test.ts"]
      manual:
        instructions: |
          1. 验证 tests/fixtures/ 目录存在
          2. 检查 fixtures 包含基础数据（behaviors, trees）
          3. 测试工厂函数可创建有效数据
          4. 验证场景数据组合可用
          5. 检查清理工具正确删除测试数据
        required_tools: ["bash", "ls", "npm"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-007~criterion-5"
    name: "代码覆盖率报告配置完成"
    description: |
      配置测试覆盖率：
      - v8 覆盖率 provider
      - 覆盖率阈值（80%）
      - HTML/Text 报告输出
      - CI 覆盖率上传
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/coverage-config.test.ts"
        test_files: ["src/infra/coverage-config.test.ts"]
      integration:
        command: "npm test -- src/infra/coverage-config.integration.test.ts"
        test_files: ["src/infra/coverage-config.integration.test.ts"]
      manual:
        instructions: |
          1. 运行 npm run test:coverage 生成报告
          2. 检查 coverage/ 目录存在 HTML 报告
          3. 验证控制台显示覆盖率摘要
          4. 测试阈值检查（低于阈值失败）
          5. 验证排除文件配置（如测试文件本身）
        required_tools: ["npm", "bash", "ls"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-007~criterion-1"]

  - id: "ISSUE-INFRA-007~criterion-6"
    name: "测试工作流脚本配置完成"
    description: |
      配置测试相关 npm 脚本：
      - test: 运行所有测试
      - test:unit: 仅单元测试
      - test:integration: 仅集成测试
      - test:watch: 监视模式
      - test:coverage: 带覆盖率
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/test-scripts.test.ts"
        test_files: ["src/infra/test-scripts.test.ts"]
      integration:
        command: "npm test -- src/infra/test-scripts.integration.test.ts"
        test_files: ["src/infra/test-scripts.integration.test.ts"]
      manual:
        instructions: |
          1. 运行各测试脚本验证功能
          2. 检查 test:unit 仅运行单元测试
          3. 验证 test:watch 监视文件变更
          4. 测试 test:coverage 生成报告
          5. 验证脚本执行时间合理
        required_tools: ["npm", "bash"]
    execution_time: "<2 minutes"
    prerequisites: ["ISSUE-INFRA-007~criterion-1", "ISSUE-INFRA-007~criterion-5"]
