id: "ISSUE-INFRA-008"
issue_number: 8
title: "数据库备份与恢复"
slug: "backup-restore"
status: "pending"
description: |
  实现数据库备份与恢复系统，支持：全量/增量备份、
  定时备份任务、备份加密、压缩存储、跨平台恢复、
  备份验证和完整性检查。
is_parent: false
prerequisites:
  - "ISSUE-INFRA-003B"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-008~criterion-1"
    name: "全量备份功能实现完成"
    description: |
      实现完整数据库备份：
      - SQLite 数据库文件复制
      - PostgreSQL pg_dump 集成
      - 备份文件命名（timestamp + workspace）
      - 备份进度显示
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/backup-full.test.ts"
        test_files: ["src/infra/backup-full.test.ts"]
      integration:
        command: "npm test -- src/infra/backup-full.integration.test.ts"
        test_files: ["src/infra/backup-full.integration.test.ts"]
      manual:
        instructions: |
          1. 运行全量备份命令
          2. 验证备份文件创建成功
          3. 检查文件大小与数据库相当
          4. 验证文件名包含时间戳
          5. 测试备份进度信息显示
        required_tools: ["npm", "bash", "ls -lh"]
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-008~criterion-2"
    name: "备份压缩和加密完成"
    description: |
      实现备份文件处理：
      - 压缩存储（gzip/zstd）
      - 加密保护（AES-256）
      - 密码/密钥管理
      - 压缩率统计
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/backup-compression.test.ts"
        test_files: ["src/infra/backup-compression.test.ts"]
      integration:
        command: "npm test -- src/infra/backup-compression.integration.test.ts"
        test_files: ["src/infra/backup-compression.integration.test.ts"]
      manual:
        instructions: |
          1. 执行备份并启用压缩
          2. 验证压缩后文件大小减少（>50%）
          3. 测试加密备份，验证密码保护
          4. 检查加密文件无法直接读取
          5. 验证压缩/加密速度可接受
        required_tools: ["npm", "bash", "gzip", "openssl"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-008~criterion-1"]

  - id: "ISSUE-INFRA-008~criterion-3"
    name: "数据库恢复功能实现完成"
    description: |
      实现数据库恢复：
      - 从备份文件恢复
      - 解压/解密处理
      - 恢复前确认提示
      - 恢复后验证
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/restore-db.test.ts"
        test_files: ["src/infra/restore-db.test.ts"]
      integration:
        command: "npm test -- src/infra/restore-db.integration.test.ts"
        test_files: ["src/infra/restore-db.integration.test.ts"]
      manual:
        instructions: |
          1. 从备份文件执行恢复
          2. 验证加密备份需要正确密码
          3. 检查恢复前确认提示
          4. 验证恢复后数据完整性
          5. 测试恢复到新位置（不覆盖原库）
        required_tools: ["npm", "bash", "sqlite3/psql"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-008~criterion-1", "ISSUE-INFRA-008~criterion-2"]

  - id: "ISSUE-INFRA-008~criterion-4"
    name: "定时备份任务配置完成"
    description: |
      实现自动备份：
      - Cron 表达式支持
      - 备份策略配置（保留 N 份）
      - 定时任务调度器
      - 失败告警通知
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/backup-scheduler.test.ts"
        test_files: ["src/infra/backup-scheduler.test.ts"]
      integration:
        command: "npm test -- src/infra/backup-scheduler.integration.test.ts"
        test_files: ["src/infra/backup-scheduler.integration.test.ts"]
      manual:
        instructions: |
          1. 配置短周期定时任务（每分钟）
          2. 验证定时任务触发备份
          3. 测试保留策略（仅保留最近 N 份）
          4. 模拟备份失败，验证告警
          5. 检查 Cron 表达式解析正确
        required_tools: ["npm", "bash"]
    execution_time: "<2 minutes"
    prerequisites: ["ISSUE-INFRA-008~criterion-1"]

  - id: "ISSUE-INFRA-008~criterion-5"
    name: "备份验证和完整性检查完成"
    description: |
      实现备份质量控制：
      - 备份文件校验和（SHA256）
      - 备份内容验证（抽样检查）
      - 恢复测试（验证可恢复性）
      - 备份状态报告
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/backup-verification.test.ts"
        test_files: ["src/infra/backup-verification.test.ts"]
      integration:
        command: "npm test -- src/infra/backup-verification.integration.test.ts"
        test_files: ["src/infra/backup-verification.integration.test.ts"]
      manual:
        instructions: |
          1. 执行备份验证命令
          2. 验证校验和文件生成
          3. 检查抽样数据验证结果
          4. 测试恢复测试流程（临时恢复）
          5. 验证备份状态报告格式
        required_tools: ["npm", "bash", "sha256sum"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-008~criterion-1", "ISSUE-INFRA-008~criterion-3"]
