id: "ISSUE-INFRA-014"
issue_number: 14
title: "Repository 模式"
slug: "repository-pattern"
status: "pending"
description: |
  实现 Repository 模式，提供统一的数据访问层：
  基础 CRUD 操作、查询构建器、事务支持、
  领域模型映射、仓储工厂、多级缓存集成。
is_parent: false
prerequisites:
  - "ISSUE-INFRA-003B"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-014~criterion-1"
    name: "基础 Repository 接口实现完成"
    description: |
      实现 Repository 基础接口：
      - BaseRepository 抽象类
      - create, findById, findAll, update, delete 方法
      - 实体映射（Entity <-> Domain Model）
      - 批量操作支持
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/repository-base.test.ts"
        test_files: ["src/infra/repository-base.test.ts"]
      integration:
        command: "npm test -- src/infra/repository-base.integration.test.ts"
        test_files: ["src/infra/repository-base.integration.test.ts"]
      manual:
        instructions: |
          1. 验证 BaseRepository 抽象类定义
          2. 测试子类实现基础 CRUD
          3. 检查实体映射正确
          4. 验证批量操作接口
          5. 测试返回领域模型非原始数据
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-014~criterion-2"
    name: "查询构建器实现完成"
    description: |
      实现查询构建器：
      - 链式 API（where, orderBy, limit, offset）
      - 复杂条件（and, or, in, between, like）
      - 关联查询（join）
      - 查询转换（toSQL）
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/query-builder.test.ts"
        test_files: ["src/infra/query-builder.test.ts"]
      integration:
        command: "npm test -- src/infra/query-builder.integration.test.ts"
        test_files: ["src/infra/query-builder.integration.test.ts"]
      manual:
        instructions: |
          1. 测试链式查询构建
          2. 验证复杂条件生成正确 SQL
          3. 检查关联查询 join 构建
          4. 测试 toSQL 输出预期 SQL
          5. 验证参数化查询防注入
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-014~criterion-3"
    name: "事务管理实现完成"
    description: |
      实现仓储事务支持：
      - 事务开始/提交/回滚
      - 事务装饰器/包装器
      - 嵌套事务处理
      - 事务传播行为
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/repository-transaction.test.ts"
        test_files: ["src/infra/repository-transaction.test.ts"]
      integration:
        command: "npm test -- src/infra/repository-transaction.integration.test.ts"
        test_files: ["src/infra/repository-transaction.integration.test.ts"]
      manual:
        instructions: |
          1. 测试事务提交后数据持久化
          2. 验证事务回滚数据恢复
          3. 检查嵌套事务行为
          4. 测试事务装饰器使用
          5. 验证事务传播配置
        required_tools: ["npm", "bash", "sqlite3/psql"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-014~criterion-4"
    name: "仓储工厂和注册表完成"
    description: |
      实现仓储管理：
      - RepositoryFactory 工厂
      - 仓储注册表
      - 依赖注入支持
      - 仓储生命周期管理
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/repository-factory.test.ts"
        test_files: ["src/infra/repository-factory.test.ts"]
      integration:
        command: "npm test -- src/infra/repository-factory.integration.test.ts"
        test_files: ["src/infra/repository-factory.integration.test.ts"]
      manual:
        instructions: |
          1. 验证 RepositoryFactory 创建仓储
          2. 测试仓储注册表查找
          3. 检查依赖注入容器集成
          4. 验证仓储单例/多例配置
          5. 测试生命周期回调
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: ["ISSUE-INFRA-014~criterion-1"]

  - id: "ISSUE-INFRA-014~criterion-5"
    name: "缓存集成完成"
    description: |
      实现仓储层缓存：
      - 查询结果缓存
      - 缓存失效策略
      - 多级缓存支持（内存+持久化）
      - 缓存统计
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/repository-cache.test.ts"
        test_files: ["src/infra/repository-cache.test.ts"]
      integration:
        command: "npm test -- src/infra/repository-cache.integration.test.ts"
        test_files: ["src/infra/repository-cache.integration.test.ts"]
      manual:
        instructions: |
          1. 验证查询结果缓存命中
          2. 测试写入后缓存失效
          3. 检查多级缓存读取顺序
          4. 验证缓存统计信息
          5. 测试缓存跳过选项
        required_tools: ["npm", "bash"]
    execution_time: "<30 seconds"
    prerequisites: []
