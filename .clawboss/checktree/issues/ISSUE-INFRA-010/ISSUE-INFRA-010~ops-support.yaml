id: "ISSUE-INFRA-010"
issue_number: 10
title: "运维支持工具"
slug: "ops-support"
status: "pending"
description: |
  实现运维支持工具集，包括：系统健康检查、性能诊断、
  数据修复工具、配置验证、日志分析助手、
  系统信息收集（用于故障排查）。
is_parent: false
prerequisites:
  - "ISSUE-INFRA-006A"
  - "ISSUE-INFRA-006B"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-010~criterion-1"
    name: "系统健康检查工具完成"
    description: |
      实现健康检查命令：
      - 数据库连接检查
      - 磁盘空间检查
      - 内存使用检查
      - 配置文件有效性检查
      - 健康状态报告输出
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/health-check.test.ts"
        test_files: ["src/infra/health-check.test.ts"]
      integration:
        command: "npm test -- src/infra/health-check.integration.test.ts"
        test_files: ["src/infra/health-check.integration.test.ts"]
      manual:
        instructions: |
          1. 运行健康检查命令（ai-me doctor）
          2. 验证数据库连接状态显示
          3. 检查磁盘空间报告
          4. 测试无效配置时的错误提示
          5. 验证健康状态退出码（健康0，异常1）
        required_tools: ["npm", "bash", "ai-me"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-010~criterion-2"
    name: "性能诊断工具完成"
    description: |
      实现性能分析命令：
      - 数据库查询性能分析
      - 内存使用分析
      - 响应时间统计
      - 瓶颈识别建议
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/perf-diagnostic.test.ts"
        test_files: ["src/infra/perf-diagnostic.test.ts"]
      integration:
        command: "npm test -- src/infra/perf-diagnostic.integration.test.ts"
        test_files: ["src/infra/perf-diagnostic.integration.test.ts"]
      manual:
        instructions: |
          1. 运行性能诊断命令
          2. 验证慢查询检测和报告
          3. 检查内存使用分析
          4. 测试瓶颈识别建议
          5. 验证性能数据可视化（如有）
        required_tools: ["npm", "bash", "ai-me"]
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-010~criterion-3"
    name: "数据修复工具完成"
    description: |
      实现数据维护命令：
      - 数据库完整性检查
      - 孤立数据清理
      - 索引重建
      - 数据修复脚本执行
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/data-repair.test.ts"
        test_files: ["src/infra/data-repair.test.ts"]
      integration:
        command: "npm test -- src/infra/data-repair.integration.test.ts"
        test_files: ["src/infra/data-repair.integration.test.ts"]
      manual:
        instructions: |
          1. 运行数据完整性检查
          2. 创建孤立数据，测试清理功能
          3. 验证索引重建命令
          4. 测试修复脚本执行（先备份后执行）
          5. 确认修复前有确认提示
        required_tools: ["npm", "bash", "ai-me"]
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-010~criterion-4"
    name: "日志分析助手完成"
    description: |
      实现日志分析命令：
      - 日志文件解析
      - 错误/警告统计
      - 时间范围筛选
      - 模式搜索和高亮
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/log-analyzer.test.ts"
        test_files: ["src/infra/log-analyzer.test.ts"]
      integration:
        command: "npm test -- src/infra/log-analyzer.integration.test.ts"
        test_files: ["src/infra/log-analyzer.integration.test.ts"]
      manual:
        instructions: |
          1. 生成测试日志文件
          2. 运行日志分析命令
          3. 验证错误/警告统计准确
          4. 测试时间范围筛选功能
          5. 检查搜索和高亮效果
        required_tools: ["npm", "bash", "ai-me"]
    execution_time: "<30 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-010~criterion-5"
    name: "系统信息收集工具完成"
    description: |
      实现系统信息收集命令（用于故障报告）：
      - 版本信息收集
      - 环境信息收集
      - 配置信息（脱敏后）
      - 最近的错误日志摘要
      - 生成诊断报告文件
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/system-info.test.ts"
        test_files: ["src/infra/system-info.test.ts"]
      integration:
        command: "npm test -- src/infra/system-info.integration.test.ts"
        test_files: ["src/infra/system-info.integration.test.ts"]
      manual:
        instructions: |
          1. 运行系统信息收集命令
          2. 验证版本信息正确
          3. 检查环境信息完整（OS, Node版本等）
          4. 确认敏感配置被脱敏
          5. 验证报告文件可生成和分享
        required_tools: ["npm", "bash", "ai-me"]
    execution_time: "<30 seconds"
    prerequisites: []
