id: "ISSUE-INFRA-005"
issue_number: 5
title: "配置管理系统"
slug: "config-system"
status: "pending"
description: |
  实现统一的配置管理系统，支持：多环境配置、配置验证、
  配置加载优先级（命令行 > 环境变量 > 配置文件 > 默认值）、
  配置热重载、敏感信息处理等。
is_parent: false
prerequisites:
  - "ISSUE-INFRA-002"
created_at: "2026-04-01"
completed_at: null
logic: sufficient_necessary
criteria:
  - id: "ISSUE-INFRA-005~criterion-1"
    name: "配置加载机制实现完成"
    description: |
      实现配置加载系统：
      - 配置文件解析（JSON, YAML, TOML）
      - 环境变量映射（如 AI_ME_DB_URL）
      - 命令行参数解析集成
      - 加载优先级：CLI > ENV > 文件 > 默认
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/config-loader.test.ts"
        test_files: ["src/infra/config-loader.test.ts"]
      integration:
        command: "npm test -- src/infra/config-loader.integration.test.ts"
        test_files: ["src/infra/config-loader.integration.test.ts"]
      manual:
        instructions: |
          1. 创建测试配置文件（config.json, config.yaml）
          2. 设置环境变量验证覆盖配置文件
          3. 使用命令行参数验证最高优先级
          4. 检查配置合并逻辑正确
          5. 验证默认值在无前述配置时生效
        required_tools: ["npm", "bash"]
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-005~criterion-2"
    name: "配置 Schema 和验证完成"
    description: |
      实现配置验证系统：
      - 配置 Schema 定义（类型、必填、范围）
      - 验证器实现（zod/yup/joi）
      - 验证错误提示和格式化
      - 配置默认值生成
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/config-validation.test.ts"
        test_files: ["src/infra/config-validation.test.ts"]
      integration:
        command: "npm test -- src/infra/config-validation.integration.test.ts"
        test_files: ["src/infra/config-validation.integration.test.ts"]
      manual:
        instructions: |
          1. 提供无效配置验证错误捕获
          2. 检查错误消息清晰（指出具体字段和问题）
          3. 验证类型检查（如数字字段拒绝字符串）
          4. 测试必填字段验证
          5. 验证范围检查（如端口号 1-65535）
        required_tools: ["npm", "bash"]
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-005~criterion-3"
    name: "多环境配置支持完成"
    description: |
      实现多环境配置管理：
      - 环境识别（development, test, production）
      - 环境特定配置文件（config.development.json）
      - NODE_ENV 自动切换
      - 环境变量覆盖机制
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/config-environments.test.ts"
        test_files: ["src/infra/config-environments.test.ts"]
      integration:
        command: "npm test -- src/infra/config-environments.integration.test.ts"
        test_files: ["src/infra/config-environments.integration.test.ts"]
      manual:
        instructions: |
          1. 创建各环境配置文件
          2. 设置 NODE_ENV=development 验证配置加载
          3. 切换 NODE_ENV=production 验证不同配置
          4. 检查测试环境自动使用内存数据库
          5. 验证环境变量可覆盖环境配置
        required_tools: ["npm", "bash"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-005~criterion-1"]

  - id: "ISSUE-INFRA-005~criterion-4"
    name: "敏感配置安全处理完成"
    description: |
      实现敏感信息的安全管理：
      - 敏感字段标记和加密存储
      - 内存中敏感数据保护
      - 配置文件权限检查（600）
      - 密钥管理服务集成接口（预留）
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/config-security.test.ts"
        test_files: ["src/infra/config-security.test.ts"]
      integration:
        command: "npm test -- src/infra/config-security.integration.test.ts"
        test_files: ["src/infra/config-security.integration.test.ts"]
      manual:
        instructions: |
          1. 验证敏感字段（password, secret, key）被标记
          2. 检查配置文件创建时权限设置为 600
          3. 验证日志中敏感信息被脱敏（***）
          4. 测试内存中敏感数据不泄露
          5. 检查密钥管理服务接口预留
        required_tools: ["npm", "bash", "ls -l"]
    execution_time: "<60 seconds"
    prerequisites: []

  - id: "ISSUE-INFRA-005~criterion-5"
    name: "配置动态更新支持完成"
    description: |
      实现配置热重载能力：
      - 文件变更监视
      - 配置重新加载
      - 变更事件通知
      - 运行时配置访问接口
    status: "pending"
    coverage:
      unit: sufficient
      integration: sufficient
      e2e: not_needed
      ai: not_needed
      manual: sufficient
    verification:
      unit:
        command: "npm test -- src/infra/config-reload.test.ts"
        test_files: ["src/infra/config-reload.test.ts"]
      integration:
        command: "npm test -- src/infra/config-reload.integration.test.ts"
        test_files: ["src/infra/config-reload.integration.test.ts"]
      manual:
        instructions: |
          1. 启动应用并加载初始配置
          2. 修改配置文件并保存
          3. 验证配置自动重新加载
          4. 检查变更事件被正确触发
          5. 验证运行时可通过 API 读取最新配置
        required_tools: ["npm", "bash"]
    execution_time: "<60 seconds"
    prerequisites: ["ISSUE-INFRA-005~criterion-1"]
